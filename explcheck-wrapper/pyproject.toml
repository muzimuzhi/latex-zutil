[project]
name = "explcheck-wrapper"
version = "0.1.0"
description = "explcheck wrapper with extra convenient CLI options"

dependencies = [
    "colorama>=0.4.6,<0.5.0",
    "tomlkit>=0.14.0,<0.15.0",
]

[project.scripts]
explcheck-wrapper = "explcheck_wrapper:main"
